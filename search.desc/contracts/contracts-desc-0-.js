searchState.loadedDescShard("contracts", 0, "A crate implementing “Design by Contract” via …\nA “contract_trait” is a trait which ensures all …\nSame as <code>ensures</code>, but uses <code>debug_assert!</code>.\nSame as <code>invariant</code>, but uses <code>debug_assert!</code>.\nSame as <code>requires</code>, but uses <code>debug_assert!</code>.\nPost-conditions are checked after the function body is run.\nInvariants are conditions that have to be maintained at …\nPre-conditions are checked before the function body is run.\nSame as <code>ensures</code>, but is only enabled in <code>#[cfg(test)]</code> …\nSame as <code>invariant</code>, but is only enabled in <code>#[cfg(test)]</code> …\nSame as <code>requires</code>, but is only enabled in <code>#[cfg(test)]</code> …")