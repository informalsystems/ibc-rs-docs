initSidebarItems({"fn":[["enable_ansi","Check if both stdout and stderr are proper terminal (tty), so that we know whether or not to enable colored output, using ANSI escape codes. If either is not, eg. because stdout is redirected to a file, we don’t enable colored output."]],"struct":[["JsonTracing","A custom component for parametrizing `tracing` in the relayer. Primarily used for:"],["PrettyTracing",""]]});