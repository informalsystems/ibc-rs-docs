window.SIDEBAR_ITEMS = {"fn":[["send_batched_messages_and_wait_check_tx",""],["send_batched_messages_and_wait_commit","Broadcast messages as multiple batched transactions to the chain all at once, and then wait for all transactions to be committed. This may improve performance in case when multiple transactions are committed into the same block. However this approach may not work if priority mempool is enabled."],["sequential_send_batched_messages_and_wait_commit","Send batched messages one after another, only after the previous one has been committed. This is only used in case if parallel transactions are committed in the wrong order due to interference from priority mempool."]]};