window.SIDEBAR_ITEMS = {"struct":[["Queue","A lightweight wrapper type to RefCell<VecDeque> so that we can safely mutate it with regular reference instead of mutable reference. We only expose subset of VecDeque methods that does not return any inner reference, so that the RefCell can never panic caused by simultaneous borrow and borrow_mut."]]};