window.SIDEBAR_ITEMS = {"fn":[["assert_same_dimension","Asserts that a nested vector `Vec<Vec<T>>` has the same dimension in its inner vectors."],["into_nested_vec","Converts a fixed-sized nested array `[[T; SIZE]; SIZE]` into a nested vector `Vec<Vec<T>>`."],["map_nested_array","Map the elements in the fixed-sized array `[[T; SIZE]; SIZE]`."],["try_into_array","Converts a dynamic-sized vector `Vec<T>` into a fixed-sized array `[T; SIZE]`. Fails if the vector is not the same length as `SIZE`."],["try_into_nested_array","Converts a dynamic-sized nested vector `Vec<Vec<T>>` into a fixed-sized nested array `[[T; SIZE]; SIZE]`. Fails if the nested vector is not of `SIZE`x`SIZE` length."]]};